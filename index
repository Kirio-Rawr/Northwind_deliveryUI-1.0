<!DOCTYPE html>
<html lang="ru">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
<title>NordWind Delivery</title>
<script src="https://telegram.org/js/telegram-web-app.js"></script>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Unbounded:wght@500;600;700;800&family=Manrope:wght@400;500;600;700;800&display=swap" rel="stylesheet">
<style>
  :root{
    --red:#D6001C;
    --red-deep:#A60015;
    --ink:#171414;
    --paper:#FAF8F7;
    --card:#FFFFFF;
    --grey:#8A8283;
    --grey-line:#EFE9E8;
    --green:#1E7A46;
    --amber:#B8720A;
    --radius-lg:24px;
    --radius-md:16px;
    --radius-sm:10px;
  }

  *{box-sizing:border-box; -webkit-tap-highlight-color:transparent;}

  html,body{margin:0; padding:0; background:var(--paper); color:var(--ink); font-family:'Manrope',sans-serif; -webkit-font-smoothing:antialiased;}
  body{display:flex; justify-content:center;}

  .screen{width:100%; max-width:480px; min-height:100vh; background:var(--paper); position:relative; padding-bottom:110px;}

  /* ===== TOP BAR ===== */
  .topbar{display:flex; align-items:center; gap:12px; padding:18px 20px 6px;}
  .back{width:36px;height:36px; border-radius:50%; background:var(--card); display:flex;align-items:center;justify-content:center; box-shadow:0 1px 3px rgba(23,20,20,0.08); flex-shrink:0; cursor:pointer;}
  .back svg{width:16px;height:16px;}
  .topbar-text{display:flex; flex-direction:column; gap:2px;}
  .topbar-title{font-family:'Unbounded'; font-size:15px; font-weight:600; letter-spacing:-0.01em;}
  .topbar-sub{font-size:12px; color:var(--grey);}

  /* ===== progress dots for form flow ===== */
  .form-progress{display:flex; gap:5px; padding:14px 20px 0;}
  .form-progress .dot{flex:1; height:4px; border-radius:3px; background:var(--grey-line);}
  .form-progress .dot.done{background:var(--red);}

  /* ===== HERO (brand header on start) ===== */
  .brandhead{margin:8px 16px 0; border-radius:var(--radius-lg); background:linear-gradient(155deg, var(--red) 0%, var(--red-deep) 78%); padding:30px 22px 26px; position:relative; overflow:hidden; color:#fff; isolation:isolate;}
  .gust{position:absolute; inset:0; z-index:-1; opacity:0.5;}
  .gust span{position:absolute; height:2px; width:46%; background:linear-gradient(90deg, rgba(255,255,255,0) 0%, rgba(255,255,255,0.9) 45%, rgba(255,255,255,0) 100%); border-radius:2px; animation:blow 3.6s ease-in-out infinite; transform:skewY(-14deg);}
  .gust span:nth-child(1){top:18%; left:-50%; animation-delay:0s;}
  .gust span:nth-child(2){top:46%; left:-70%; width:60%; animation-delay:0.9s; opacity:0.6;}
  .gust span:nth-child(3){top:72%; left:-40%; width:34%; animation-delay:1.8s; opacity:0.4;}
  @keyframes blow{0%{transform:translateX(0) skewY(-14deg); opacity:0;} 12%{opacity:1;} 75%{opacity:0.3;} 100%{transform:translateX(260%) skewY(-14deg); opacity:0;}}
  @media (prefers-reduced-motion: reduce){.gust span{animation:none; display:none;}}

  .brand-mark{width:38px; height:26px; margin-bottom:14px;}
  .brand-title{font-family:'Unbounded'; font-size:22px; font-weight:700; letter-spacing:-0.01em; margin:0 0 6px;}
  .brand-sub{font-size:13.5px; color:rgba(255,255,255,0.85); font-weight:500; line-height:1.4;}

  /* ===== MENU CARDS (start screen) ===== */
  .menu-list{padding:18px 16px 0; display:flex; flex-direction:column; gap:10px;}
  .menu-card{background:var(--card); border-radius:var(--radius-md); padding:16px; display:flex; align-items:center; gap:14px; box-shadow:0 1px 3px rgba(23,20,20,0.05); cursor:pointer; border:none; width:100%; text-align:left; font-family:'Manrope';}
  .menu-card .ic{width:44px;height:44px; border-radius:12px; background:var(--paper); display:flex;align-items:center;justify-content:center; font-size:22px; flex-shrink:0;}
  .menu-card .txt{flex:1;}
  .menu-card .title{font-size:14.5px; font-weight:800;}
  .menu-card .sub{font-size:12px; color:var(--grey); margin-top:2px;}
  .menu-card .chev{color:var(--grey);}

  /* ===== SECTION ===== */
  .section-title{font-size:12px; font-weight:800; color:var(--grey); text-transform:uppercase; letter-spacing:0.06em; margin:26px 20px 10px;}

  /* ===== STEP TITLE ===== */
  .step-title{font-family:'Unbounded'; font-size:20px; font-weight:700; letter-spacing:-0.01em; margin:18px 20px 4px;}
  .step-sub{font-size:13px; color:var(--grey); margin:0 20px 18px; line-height:1.4;}

  /* ===== SHOPS GRID ===== */
  .shops{padding:0 16px; display:grid; grid-template-columns:1fr 1fr; gap:10px;}
  .shop-card{background:var(--card); border-radius:var(--radius-md); padding:16px 12px; text-align:center; box-shadow:0 1px 3px rgba(23,20,20,0.05); border:2px solid transparent; cursor:pointer;}
  .shop-card.selected{border-color:var(--red); background:#FFF3F4;}
  .shop-card .emoji{font-size:28px; margin-bottom:8px;}
  .shop-card .name{font-size:13px; font-weight:700;}

  /* ===== FORM FIELDS ===== */
  .field-wrap{padding:0 16px;}
  textarea, input[type=text], input[type=tel]{
    width:100%; background:var(--card); border:1.5px solid var(--grey-line); border-radius:var(--radius-md);
    padding:14px 16px; font-family:'Manrope'; font-size:14px; color:var(--ink); font-weight:600;
    outline:none; resize:none;
  }
  textarea:focus, input:focus{border-color:var(--red);}
  textarea{min-height:120px; line-height:1.5;}
  textarea::placeholder, input::placeholder{color:var(--grey); font-weight:500;}

  .hint-box{margin:12px 16px 0; background:var(--card); border-radius:var(--radius-md); padding:13px 16px; font-size:12.5px; color:var(--grey); line-height:1.5;}
  .hint-box b{color:var(--ink);}

  /* photo upload */
  .photo-zone{margin:0 16px; border:2px dashed var(--grey-line); border-radius:var(--radius-md); padding:28px 16px; text-align:center; cursor:pointer; background:var(--card);}
  .photo-zone .ic{font-size:28px; margin-bottom:8px;}
  .photo-zone .t{font-size:13px; font-weight:700;}
  .photo-zone .s{font-size:11.5px; color:var(--grey); margin-top:3px;}
  .photo-preview{margin:0 16px; border-radius:var(--radius-md); overflow:hidden; position:relative;}
  .photo-preview img{width:100%; display:block; max-height:220px; object-fit:cover;}
  .photo-remove{position:absolute; top:10px; right:10px; width:30px;height:30px; border-radius:50%; background:rgba(23,20,20,0.6); display:flex;align-items:center;justify-content:center; color:#fff; font-size:14px; cursor:pointer;}
  .skip-link{text-align:center; margin-top:14px; font-size:12.5px; font-weight:700; color:var(--grey); text-decoration:underline; cursor:pointer;}

  /* ===== SUMMARY (confirm screen) ===== */
  .details{margin:0 16px; background:var(--card); border-radius:var(--radius-md); box-shadow:0 1px 3px rgba(23,20,20,0.05); overflow:hidden;}
  .detail-row{display:flex; justify-content:space-between; align-items:flex-start; gap:14px; padding:13px 16px; border-bottom:1px solid var(--grey-line); font-size:13px;}
  .detail-row:last-child{border-bottom:none;}
  .detail-label{color:var(--grey); font-weight:600; flex-shrink:0;}
  .detail-value{font-weight:700; text-align:right;}

  /* ===== FOOTER ===== */
  .footer{position:fixed; bottom:0; left:0; right:0; display:flex; justify-content:center; padding:14px 16px calc(14px + env(safe-area-inset-bottom)); background:linear-gradient(to top, var(--paper) 60%, rgba(250,248,247,0));}
  .footer-inner{width:100%; max-width:480px; padding:0 4px; display:flex; gap:10px;}
  .btn-primary{flex:1; border:none; background:var(--ink); color:#fff; font-family:'Manrope'; font-weight:700; font-size:14.5px; padding:16px; border-radius:16px; display:flex; align-items:center; justify-content:center; gap:8px; cursor:pointer;}
  .btn-primary:disabled{opacity:0.35; pointer-events:none;}
  .btn-primary:active{opacity:0.85;}
  .btn-secondary{width:52px; border:1.5px solid var(--grey-line); background:var(--card); color:var(--ink); border-radius:16px; display:flex; align-items:center; justify-content:center; cursor:pointer;}

  /* ===== SUCCESS SCREEN ===== */
  .success-wrap{padding:60px 24px 0; text-align:center;}
  .success-ic{width:84px; height:84px; border-radius:50%; background:linear-gradient(155deg, var(--red) 0%, var(--red-deep) 100%); display:flex; align-items:center; justify-content:center; margin:0 auto 22px; color:#fff;}
  .success-ic svg{width:38px;height:38px;}
  .success-title{font-family:'Unbounded'; font-size:21px; font-weight:700; margin:0 0 8px;}
  .success-sub{font-size:13.5px; color:var(--grey); line-height:1.5; max-width:300px; margin:0 auto;}

  /* ===== ORDER STATUS SCREEN ===== */
  .hero{margin:0 16px; border-radius:var(--radius-lg); background:linear-gradient(155deg, var(--red) 0%, var(--red-deep) 78%); padding:26px 22px 24px; position:relative; overflow:hidden; color:#fff; isolation:isolate;}
  .order-row{display:flex; justify-content:space-between; align-items:center; font-size:12.5px; color:rgba(255,255,255,0.78); margin-bottom:14px; font-weight:600; letter-spacing:0.02em;}
  .status-title{font-family:'Unbounded'; font-size:24px; font-weight:700; letter-spacing:-0.01em; margin:0 0 6px;}
  .status-eta{font-size:13.5px; color:rgba(255,255,255,0.85); margin:0 0 22px; font-weight:500;}
  .status-eta b{font-weight:800; color:#fff;}
  .track{display:flex; align-items:center; gap:0;}
  .track .seg{flex:1; height:4px; background:rgba(255,255,255,0.25); border-radius:3px; margin:0 3px; position:relative; overflow:hidden;}
  .track .seg.done{background:rgba(255,255,255,0.95);}
  .track .seg.active::after{content:""; position:absolute; inset:0; background:#fff; animation:fill 1.4s ease-in-out infinite; transform-origin:left;}
  @keyframes fill{0%{transform:scaleX(0);} 60%{transform:scaleX(1);} 100%{transform:scaleX(1); opacity:0.4;}}
  .steps-labels{display:flex; justify-content:space-between; margin-top:10px; font-size:10.5px; font-weight:700; color:rgba(255,255,255,0.65); letter-spacing:0.01em;}
  .steps-labels span.on{color:#fff;}
  .late-banner{margin:14px 16px 0; background:#FFF4E3; border:1px solid #F3D9A6; border-radius:var(--radius-md); padding:13px 16px; display:flex; gap:10px; align-items:flex-start; font-size:13px; color:#7A4E06; font-weight:600; line-height:1.4;}
  .late-banner .dot{width:8px;height:8px;border-radius:50%; background:var(--amber); flex-shrink:0; margin-top:5px;}
  .courier{margin:16px 16px 0; background:var(--card); border-radius:var(--radius-md); padding:14px 16px; display:flex; align-items:center; gap:12px; box-shadow:0 1px 3px rgba(23,20,20,0.05);}
  .courier-avatar{width:42px;height:42px;border-radius:50%; background:var(--red); display:flex;align-items:center;justify-content:center; flex-shrink:0;}
  .courier-avatar svg{width:20px;height:20px;}
  .courier-info{flex:1;}
  .courier-name{font-size:13.5px; font-weight:700;}
  .courier-role{font-size:12px; color:var(--grey); margin-top:1px;}
  .courier-call{width:36px;height:36px;border-radius:50%; background:var(--paper); display:flex;align-items:center;justify-content:center;}
  .courier-call svg{width:16px;height:16px;}
  .demo-bar{display:flex; gap:6px; padding:14px 16px 0; overflow-x:auto; scrollbar-width:none;}
  .demo-bar::-webkit-scrollbar{display:none;}
  .demo-chip{flex:0 0 auto; font-size:10.5px; font-weight:700; padding:7px 12px; border-radius:20px; background:var(--card); border:1px solid var(--grey-line); color:var(--grey); cursor:pointer;}
  .demo-chip.active{background:var(--ink); color:#fff; border-color:var(--ink);}

  .view{display:none;}
  .view.active{display:block;}
</style>
</head>
<body>

<div class="screen">

  <!-- ============ VIEW: START ============ -->
  <div class="view active" id="view-start">
    <div class="brandhead">
      <div class="gust"><span></span><span></span><span></span></div>
      <svg class="brand-mark" viewBox="0 0 200 130" fill="none"><path d="M20 90 C20 60, 45 45, 75 45 L100 45 L75 90 C75 90, 100 90, 125 45 L150 45 C180 45, 180 90, 180 90" stroke="#fff" stroke-width="14" stroke-linecap="round" stroke-linejoin="round" fill="none"/></svg>
      <div class="brand-title">NordWind Delivery</div>
      <div class="brand-sub">Доставка продуктов из любимых магазинов — быстро и без звонков</div>
    </div>

    <div class="menu-list">
      <button class="menu-card" onclick="go('view-shop')">
        <div class="ic">🛒</div>
        <div class="txt"><div class="title">Сделать заказ</div><div class="sub">Выбрать магазин и собрать список покупок</div></div>
        <div class="chev">›</div>
      </button>
      <button class="menu-card" onclick="showMyOrder()">
        <div class="ic">📦</div>
        <div class="txt"><div class="title">Мой заказ</div><div class="sub">Отследить статус текущей доставки</div></div>
        <div class="chev">›</div>
      </button>
    </div>
  </div>

  <!-- ============ VIEW: SHOP ============ -->
  <div class="view" id="view-shop">
    <div class="topbar">
      <div class="back" onclick="go('view-start')"><svg viewBox="0 0 24 24" fill="none"><path d="M15 18L9 12L15 6" stroke="#171414" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"/></svg></div>
      <div class="topbar-text"><div class="topbar-title">Новый заказ</div><div class="topbar-sub">Шаг 1 из 6</div></div>
    </div>
    <div class="form-progress" id="progress-shop"></div>
    <div class="step-title">Выберите магазин</div>
    <div class="step-sub">Мы соберём заказ именно там</div>
    <div class="shops" id="shopsGrid">
      <div class="shop-card" data-shop="🟢 Чижик" onclick="pickShop(this)"><div class="emoji">🟢</div><div class="name">Чижик</div></div>
      <div class="shop-card" data-shop="🔵 Ашан" onclick="pickShop(this)"><div class="emoji">🔵</div><div class="name">Ашан</div></div>
      <div class="shop-card" data-shop="🟠 Дикси" onclick="pickShop(this)"><div class="emoji">🟠</div><div class="name">Дикси</div></div>
      <div class="shop-card" data-shop="🔴 Пятёрочка" onclick="pickShop(this)"><div class="emoji">🔴</div><div class="name">Пятёрочка</div></div>
      <div class="shop-card" data-shop="🟡 Магнит" onclick="pickShop(this)"><div class="emoji">🟡</div><div class="name">Магнит</div></div>
    </div>
    <div class="footer"><div class="footer-inner">
      <button class="btn-primary" id="shopNext" disabled onclick="go('view-products')">Продолжить</button>
    </div></div>
  </div>

  <!-- ============ VIEW: PRODUCTS ============ -->
  <div class="view" id="view-products">
    <div class="topbar">
      <div class="back" onclick="go('view-shop')"><svg viewBox="0 0 24 24" fill="none"><path d="M15 18L9 12L15 6" stroke="#171414" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"/></svg></div>
      <div class="topbar-text"><div class="topbar-title">Новый заказ</div><div class="topbar-sub">Шаг 2 из 6</div></div>
    </div>
    <div class="form-progress" id="progress-products"></div>
    <div class="step-title">Что нужно купить?</div>
    <div class="step-sub">Одна позиция — одна строка</div>
    <div class="field-wrap">
      <textarea id="productsInput" placeholder="Молоко 2 шт&#10;Хлеб&#10;Яблоки"></textarea>
    </div>
    <div class="footer"><div class="footer-inner">
      <button class="btn-primary" id="productsNext" disabled onclick="go('view-photo')">Продолжить</button>
    </div></div>
  </div>

  <!-- ============ VIEW: PHOTO ============ -->
  <div class="view" id="view-photo">
    <div class="topbar">
      <div class="back" onclick="go('view-products')"><svg viewBox="0 0 24 24" fill="none"><path d="M15 18L9 12L15 6" stroke="#171414" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"/></svg></div>
      <div class="topbar-text"><div class="topbar-title">Новый заказ</div><div class="topbar-sub">Шаг 3 из 6</div></div>
    </div>
    <div class="form-progress" id="progress-photo"></div>
    <div class="step-title">Фото товара</div>
    <div class="step-sub">Необязательно — поможет точнее собрать заказ</div>

    <input type="file" id="photoFile" accept="image/*" style="display:none" onchange="onPhoto(event)">
    <div class="photo-zone" id="photoZone" onclick="document.getElementById('photoFile').click()">
      <div class="ic">📸</div>
      <div class="t">Добавить фото</div>
      <div class="s">JPG, PNG — до 10 МБ</div>
    </div>
    <div class="photo-preview" id="photoPreview" style="display:none;">
      <img id="photoImg" src="">
      <div class="photo-remove" onclick="removePhoto()">✕</div>
    </div>
    <div class="skip-link" onclick="go('view-time')">Пропустить этот шаг</div>

    <div class="footer"><div class="footer-inner">
      <button class="btn-primary" onclick="go('view-time')">Продолжить</button>
    </div></div>
  </div>

  <!-- ============ VIEW: TIME ============ -->
  <div class="view" id="view-time">
    <div class="topbar">
      <div class="back" onclick="go('view-photo')"><svg viewBox="0 0 24 24" fill="none"><path d="M15 18L9 12L15 6" stroke="#171414" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"/></svg></div>
      <div class="topbar-text"><div class="topbar-title">Новый заказ</div><div class="topbar-sub">Шаг 4 из 6</div></div>
    </div>
    <div class="form-progress" id="progress-time"></div>
    <div class="step-title">К какому времени?</div>
    <div class="step-sub">Например: «к 19:00» или «как можно скорее»</div>
    <div class="field-wrap">
      <input type="text" id="timeInput" placeholder="К 19:00">
    </div>
    <div class="footer"><div class="footer-inner">
      <button class="btn-primary" id="timeNext" disabled onclick="go('view-address')">Продолжить</button>
    </div></div>
  </div>

  <!-- ============ VIEW: ADDRESS ============ -->
  <div class="view" id="view-address">
    <div class="topbar">
      <div class="back" onclick="go('view-time')"><svg viewBox="0 0 24 24" fill="none"><path d="M15 18L9 12L15 6" stroke="#171414" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"/></svg></div>
      <div class="topbar-text"><div class="topbar-title">Новый заказ</div><div class="topbar-sub">Шаг 5 из 6</div></div>
    </div>
    <div class="form-progress" id="progress-address"></div>
    <div class="step-title">Куда привезти?</div>
    <div class="step-sub">Улица, дом, квартира, домофон — если есть</div>
    <div class="field-wrap">
      <input type="text" id="addressInput" placeholder="пр. Северный, д. 14, кв. 27">
    </div>
    <div class="footer"><div class="footer-inner">
      <button class="btn-primary" id="addressNext" disabled onclick="go('view-phone')">Продолжить</button>
    </div></div>
  </div>

  <!-- ============ VIEW: PHONE ============ -->
  <div class="view" id="view-phone">
    <div class="topbar">
      <div class="back" onclick="go('view-address')"><svg viewBox="0 0 24 24" fill="none"><path d="M15 18L9 12L15 6" stroke="#171414" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"/></svg></div>
      <div class="topbar-text"><div class="topbar-title">Новый заказ</div><div class="topbar-sub">Шаг 6 из 6</div></div>
    </div>
    <div class="form-progress" id="progress-phone"></div>
    <div class="step-title">Номер телефона</div>
    <div class="step-sub">Курьер свяжется, если что-то уточнить</div>
    <div class="field-wrap">
      <input type="tel" id="phoneInput" placeholder="+7 900 000-00-00">
    </div>
    <div class="footer"><div class="footer-inner">
      <button class="btn-primary" id="phoneNext" disabled onclick="showSummary()">Проверить заказ</button>
    </div></div>
  </div>

  <!-- ============ VIEW: SUMMARY ============ -->
  <div class="view" id="view-summary">
    <div class="topbar">
      <div class="back" onclick="go('view-phone')"><svg viewBox="0 0 24 24" fill="none"><path d="M15 18L9 12L15 6" stroke="#171414" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"/></svg></div>
      <div class="topbar-text"><div class="topbar-title">Проверьте заказ</div><div class="topbar-sub">Всё верно?</div></div>
    </div>
    <div class="section-title">Детали заказа</div>
    <div class="details" id="summaryBox"></div>
    <div class="footer"><div class="footer-inner">
      <button class="btn-secondary" onclick="go('view-shop')">✎</button>
      <button class="btn-primary" onclick="submitOrder()">Оформить заказ</button>
    </div></div>
  </div>

  <!-- ============ VIEW: SUCCESS ============ -->
  <div class="view" id="view-success">
    <div class="success-wrap">
      <div class="success-ic"><svg viewBox="0 0 24 24" fill="none"><path d="M5 13l4 4L19 7" stroke="#fff" stroke-width="2.6" stroke-linecap="round" stroke-linejoin="round"/></svg></div>
      <div class="success-title">Заказ №<span id="successOrderNo">1042</span> создан</div>
      <div class="success-sub">Магазин уже получил ваш список. Следите за статусом в разделе «Мой заказ»</div>
    </div>
    <div class="footer"><div class="footer-inner">
      <button class="btn-primary" onclick="showStatus()">Отследить заказ</button>
    </div></div>
  </div>

  <!-- ============ VIEW: STATUS ============ -->
  <div class="view" id="view-status">
    <div class="topbar">
      <div class="back" onclick="go('view-start')"><svg viewBox="0 0 24 24" fill="none"><path d="M15 18L9 12L15 6" stroke="#171414" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"/></svg></div>
      <div class="topbar-text"><div class="topbar-title">Заказ №<span id="orderNo">1042</span></div><div class="topbar-sub">NordWind Delivery · оформлен в <span id="orderTime">19:47</span></div></div>
    </div>

    <div class="hero">
      <div class="gust"><span></span><span></span><span></span></div>
      <div class="order-row"><span>ПУТЬ ЗАКАЗА</span><span id="etaBadge">до 20:35</span></div>
      <div class="status-title" id="statusTitle">Магазин собирает заказ</div>
      <div class="status-eta" id="statusSub">Если не успеем к сроку — подарим промокод на <b>−20%</b></div>
      <div class="track" id="track">
        <div class="seg done"></div><div class="seg active"></div><div class="seg"></div><div class="seg"></div>
      </div>
      <div class="steps-labels">
        <span class="on">Принят</span><span class="on">Собирают</span><span>Везём</span><span>Доставлен</span>
      </div>
    </div>

    <div class="late-banner" id="lateBanner" style="display:none;">
      <div class="dot"></div>
      <div>Курьер задерживается из‑за трафика — новое время прибытия уточняем, спасибо за терпение</div>
    </div>

    <div class="courier" id="courierCard" style="display:none;">
      <div class="courier-avatar"><svg viewBox="0 0 24 24" fill="none"><path d="M4 17V9.5L12 4l8 5.5V17" stroke="#fff" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/><path d="M9 20v-6h6v6" stroke="#fff" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg></div>
      <div class="courier-info"><div class="courier-name" id="courierName">Курьер NordWind</div><div class="courier-role" id="courierRole">Уже в пути к вам</div></div>
      <div class="courier-call"><svg viewBox="0 0 24 24" fill="none"><path d="M6.6 10.8a15 15 0 0 0 6.6 6.6l2.2-2.2a1 1 0 0 1 1-.25c1.1.36 2.28.56 3.5.56a1 1 0 0 1 1 1V20a1 1 0 0 1-1 1C10.9 21 3 13.1 3 3.5A1 1 0 0 1 4 2.5h3.5a1 1 0 0 1 1 1c0 1.22.2 2.4.56 3.5a1 1 0 0 1-.25 1L6.6 10.8Z" stroke="#171414" stroke-width="1.8" stroke-linejoin="round"/></svg></div>
    </div>

    <div class="section-title">Детали заказа</div>
    <div class="details" id="statusDetails"></div>

    <div class="footer"><div class="footer-inner">
      <button class="btn-primary" id="mainBtn">Написать в поддержку</button>
    </div></div>
  </div>

</div>

<script>
  try{ window.Telegram.WebApp.expand(); window.Telegram.WebApp.ready(); }catch(e){}

  const order = { shop:null, products:"", photo:null, time:"", address:"", phone:"" };
  const stepOrder = ["view-shop","view-products","view-photo","view-time","view-address","view-phone"];

  function go(id){
    document.querySelectorAll('.view').forEach(v=>v.classList.remove('active'));
    document.getElementById(id).classList.add('active');
    renderProgress(id);
  }

  function renderProgress(id){
    const idx = stepOrder.indexOf(id);
    if(idx === -1) return;
    const bar = document.getElementById('progress-' + id.replace('view-',''));
    if(!bar) return;
    bar.innerHTML = "";
    for(let i=0;i<stepOrder.length;i++){
      const d = document.createElement('div');
      d.className = 'dot' + (i <= idx ? ' done' : '');
      bar.appendChild(d);
    }
  }

  // ---- shop ----
  function pickShop(el){
    document.querySelectorAll('.shop-card').forEach(c=>c.classList.remove('selected'));
    el.classList.add('selected');
    order.shop = el.dataset.shop;
    document.getElementById('shopNext').disabled = false;
  }

  // ---- products ----
  document.getElementById('productsInput').addEventListener('input', function(){
    order.products = this.value.trim();
    document.getElementById('productsNext').disabled = order.products.length === 0;
  });

  // ---- photo ----
  function onPhoto(e){
    const file = e.target.files[0];
    if(!file) return;
    const reader = new FileReader();
    reader.onload = function(ev){
      order.photo = ev.target.result;
      document.getElementById('photoImg').src = ev.target.result;
      document.getElementById('photoZone').style.display = 'none';
      document.getElementById('photoPreview').style.display = 'block';
    };
    reader.readAsDataURL(file);
  }
  function removePhoto(){
    order.photo = null;
    document.getElementById('photoFile').value = "";
    document.getElementById('photoZone').style.display = 'block';
    document.getElementById('photoPreview').style.display = 'none';
  }

  // ---- time ----
  document.getElementById('timeInput').addEventListener('input', function(){
    order.time = this.value.trim();
    document.getElementById('timeNext').disabled = order.time.length === 0;
  });

  // ---- address ----
  document.getElementById('addressInput').addEventListener('input', function(){
    order.address = this.value.trim();
    document.getElementById('addressNext').disabled = order.address.length === 0;
  });

  // ---- phone ----
  document.getElementById('phoneInput').addEventListener('input', function(){
    order.phone = this.value.trim();
    document.getElementById('phoneNext').disabled = order.phone.length < 5;
  });

  // ---- summary ----
  function showSummary(){
    const rows = [
      ["Магазин", order.shop],
      ["Товары", order.products.replace(/\n/g,', ')],
      ["Фото", order.photo ? "Прикреплено" : "Не прикреплено"],
      ["Время доставки", order.time],
      ["Адрес", order.address],
      ["Телефон", order.phone]
    ];
    document.getElementById('summaryBox').innerHTML = rows.map(r =>
      `<div class="detail-row"><span class="detail-label">${r[0]}</span><span class="detail-value">${r[1]}</span></div>`
    ).join('');
    go('view-summary');
  }

  // ---- submit ----
  function submitOrder(){
    const payload = { type:"new_order", ...order };
    try{
      window.Telegram.WebApp.sendData(JSON.stringify(payload));
    }catch(e){
      console.log("Demo mode, order payload:", payload);
    }
    const orderNo = Math.floor(1000 + Math.random()*9000);
    document.getElementById('successOrderNo').textContent = orderNo;
    document.getElementById('orderNo').textContent = orderNo;
    const now = new Date();
    const hh = String(now.getHours()).padStart(2,'0');
    const mm = String(now.getMinutes()).padStart(2,'0');
    document.getElementById('orderTime').textContent = hh + ':' + mm;
    go('view-success');
  }

  function showMyOrder(){
    renderStatusDetails();
    go('view-status');
    render('collecting');
  }
  function showStatus(){
    renderStatusDetails();
    go('view-status');
    render('accepted');
  }

  function renderStatusDetails(){
    document.getElementById('statusDetails').innerHTML = `
      <div class="detail-row"><span class="detail-label">Магазин</span><span class="detail-value">${order.shop || '🟢 Чижик'}</span></div>
      <div class="detail-row"><span class="detail-label">Адрес доставки</span><span class="detail-value">${order.address || 'пр. Северный, д. 14'}</span></div>
      <div class="detail-row"><span class="detail-label">Ожидаемое время</span><span class="detail-value">${order.time || 'до 20:35'}</span></div>
    `;
  }

  // ---- status demo states ----
  const SUPPORT_USERNAME = "nordwind_support"; // замени на реальный юзернейм поддержки в Telegram
  const COURIER_PHONE = "+79264550612";

  const STATES = {
    accepted:   { title:"Заказ принят", sub:"Магазин уже получил ваш заказ и готовит его к сборке", fill:1, banner:false, courier:false, btn:"Написать в поддержку", action:"support" },
    collecting: { title:"Магазин собирает заказ", sub:"Если не успеем к сроку — подарим промокод на <b>−20%</b>", fill:2, banner:false, courier:false, btn:"Написать в поддержку", action:"support" },
    delivery:   { title:"Курьер везёт заказ", sub:"Уже в пути — можно следить за курьером ниже", fill:3, banner:false, courier:true, courierText:"Уже в пути к вам", btn:"Связаться с курьером", action:"call" },
    waiting:    { title:"Курьер ждёт вас", sub:"Курьер на месте и ждёт у двери", fill:3, banner:false, courier:true, courierText:"Ждёт у двери", btn:"Связаться с курьером", action:"call" },
    late:       { title:"Курьер везёт заказ", sub:"Уточняем новое время прибытия", fill:3, banner:true, courier:true, courierText:"В пути, но задерживается", btn:"Связаться с курьером", action:"call" },
    delivered:  { title:"Заказ доставлен", sub:"Спасибо, что выбрали <b>NordWind Delivery</b> 🌬️", fill:4, banner:false, courier:false, btn:null, action:"none" }
  };

  let currentAction = "support";

  function render(key){
    const s = STATES[key];
    const segs = document.querySelectorAll('#track .seg');
    const labels = document.querySelectorAll('.steps-labels span');
    document.getElementById('statusTitle').textContent = s.title;
    document.getElementById('statusSub').innerHTML = s.sub;
    document.getElementById('lateBanner').style.display = s.banner ? 'flex' : 'none';
    document.getElementById('courierCard').style.display = s.courier ? 'flex' : 'none';
    if(s.courier) document.getElementById('courierRole').textContent = s.courierText;
    document.getElementById('mainBtn').textContent = s.btn;
    document.getElementById('mainBtn').style.display = s.btn ? 'flex' : 'none';
    currentAction = s.action;
    segs.forEach((seg, i) => {
      seg.classList.remove('done','active');
      if(i < s.fill - 1) seg.classList.add('done');
      else if(i === s.fill - 1) seg.classList.add(s.fill === 4 ? 'done' : 'active');
    });
    labels.forEach((lab, i) => lab.classList.toggle('on', i <= s.fill - 1));
    document.querySelectorAll('.demo-chip').forEach(c => c.classList.toggle('active', c.dataset.s === key));
  }
  document.querySelectorAll('.demo-chip').forEach(chip => chip.addEventListener('click', () => render(chip.dataset.s)));

  document.getElementById('mainBtn').addEventListener('click', function(){
    if(currentAction === "support"){
      try{
        window.Telegram.WebApp.openTelegramLink("https://t.me/" + SUPPORT_USERNAME);
      }catch(e){
        window.open("https://t.me/" + SUPPORT_USERNAME, "_blank");
      }
    } else if(currentAction === "call"){
      window.location.href = "tel:" + COURIER_PHONE;
    }
  });
</script>

</body>
</html>
